import { reactSettingsManager } from './react-settings-manager.js';

describe('reactSettingsManager', () => {
  it('should work', () => {
    expect(reactSettingsManager()).toEqual('react-settings-manager');
  });
});
